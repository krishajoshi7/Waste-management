/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "localhost",
    "127.0.0.1",
    "exorcism-cloning-collie.ngrok-free.dev"
  ]
};

module.exports = nextConfig;
